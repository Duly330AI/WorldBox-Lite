{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "state_spec.schema.json",
  "title": "State Spec",
  "type": "object",
  "additionalProperties": false,
  "required": ["spec_id", "version", "memory_layout", "entity_state"],
  "properties": {
    "spec_id": { "type": "string" },
    "version": { "type": "string" },
    "schema_version": { "type": "string" },
    "memory_layout": {
      "type": "object",
      "additionalProperties": false,
      "required": ["terrain_buffer", "feature_buffer", "building_buffer", "building_storage_buffer", "height_buffer", "explored_buffer"],
      "properties": {
        "terrain_buffer": { "$ref": "#/definitions/bufferDef" },
        "feature_buffer": { "$ref": "#/definitions/bufferDef" },
        "building_buffer": { "$ref": "#/definitions/bufferDef" },
        "building_storage_buffer": { "$ref": "#/definitions/bufferDef" },
        "height_buffer": { "$ref": "#/definitions/bufferDef" },
        "explored_buffer": { "$ref": "#/definitions/bufferDef" }
      }
    },
    "entity_state": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_entities", "properties_per_entity"],
      "properties": {
        "max_entities": { "type": "integer", "minimum": 1 },
        "properties_per_entity": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "id",
            "type",
            "faction_id",
            "x",
            "y",
            "health",
            "energy",
            "hunger",
            "inventory_wood",
            "inventory_food",
            "target_x",
            "target_y",
            "current_action_id",
            "action_progress",
            "plan_lock_ticks",
            "animation_frame"
          ],
          "properties": {
            "id": { "$ref": "#/definitions/entityType" },
            "type": { "$ref": "#/definitions/entityType" },
            "faction_id": { "$ref": "#/definitions/entityType" },
            "x": { "$ref": "#/definitions/entityType" },
            "y": { "$ref": "#/definitions/entityType" },
            "health": { "$ref": "#/definitions/entityType" },
            "energy": { "$ref": "#/definitions/entityType" },
            "hunger": { "$ref": "#/definitions/entityType" },
            "inventory_wood": { "$ref": "#/definitions/entityType" },
            "inventory_food": { "$ref": "#/definitions/entityType" },
            "target_x": { "$ref": "#/definitions/entityType" },
            "target_y": { "$ref": "#/definitions/entityType" },
            "current_action_id": { "$ref": "#/definitions/entityType" },
            "action_progress": { "$ref": "#/definitions/entityType" },
            "plan_lock_ticks": { "$ref": "#/definitions/entityType" },
            "animation_frame": { "$ref": "#/definitions/entityType" }
          }
        }
      }
    }
  },
  "definitions": {
    "bufferDef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "bytes_per_tile"],
      "properties": {
        "type": { "type": "string" },
        "bytes_per_tile": { "type": "integer", "minimum": 1 },
        "description": { "type": "string" }
      }
    },
    "entityType": {
      "type": "string",
      "enum": ["uint8", "uint16", "uint32"]
    }
  }
}
